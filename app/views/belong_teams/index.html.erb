<div class="belong-teams-index">
  <br>
  <h2 class="belong-teams-title">マイチーム</h2>
  <div class="belong-teams-card">
    <% unless @teams.blank? %>
      <% @teams.each do |team|%>
        <div class="row">
          <%= link_to(belong_team_path(team), class: "belong-teams-link") do %>
            <%= image_tag team.image.url(:medium), class: "belong-teams-round-img" %>
            <span><%= markdown(team.name) %></span>
            <p ><%= markdown(team.remarks.truncate(50)) %></p>
            <p class="team-time no-text-decoration"><%= "#{time_ago_in_words(team.updated_at).upcase}前" %></p> 
          <% end %>
        </div>
      <% end %>
    <% else %>
      <h3>どのチームにも所属しておりません！<br>チームから参加したいチームを選びましょう！</h3>
      <h3>
    <% end %>
  </div>
</div>